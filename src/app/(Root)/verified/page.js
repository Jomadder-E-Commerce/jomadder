import Verified from '@/components/pages/verification/Verified';
import React from 'react';

const page = () => {
    return (
        <div>
            <Verified/>
        </div>
    );
};

export default page;